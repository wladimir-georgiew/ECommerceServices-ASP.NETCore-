@model string[]

    <div class="form-group">
        <label>Total Salary</label>
        <input disabled class="form-control" value="@Model[0]$" />
    </div>
    <div class="form-group">
        <label>Pending Bonuses for @Model[2]</label>
        <p style="color: red; font-size: 10px; font-weight: 400">*will be added to the salary at the 1st day of next month (@Model[3])*</p>
        <input disabled class="form-control" value="@Model[1]$" />
    </div>